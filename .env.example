# ===========================================
# Application (required)
# ===========================================
NODE_ENV=development
PORT=3000

# ===========================================
# Database (required)
# ===========================================
DATABASE_URL=sqlserver://localhost:1433;database=myapp;user=sa;password=YourPassword;encrypt=true;trustServerCertificate=true;

# ===========================================
# Docker Compose (used when running docker-compose)
# ===========================================
# DB_SA_PASSWORD=YourPassword
# DB_NAME=myapp
# DB_PORT=1433

# ===========================================
# SMTP (optional – leave empty to disable)
# ===========================================
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@localhost

# ===========================================
# FTP (optional)
# ===========================================
FTP_HOST=
FTP_PORT=21
FTP_USER=
FTP_PASSWORD=
FTP_SECURE=false

# ===========================================
# Microsoft Entra ID (optional – for /api/auth/entra/*)
# ===========================================
ENTRA_CLIENT_ID=
ENTRA_CLIENT_SECRET=
ENTRA_TENANT_ID=
ENTRA_REDIRECT_URI=http://localhost:3000/api/auth/entra/callback

# Where to redirect after OAuth callback (token in URL fragment). Optional; if unset, callback returns JSON.
CLIENT_REDIRECT_URI=http://localhost:3000/api/auth/demo
